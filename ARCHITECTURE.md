# ManStyle AI 系统架构设计

## 1. 总体架构

ManStyle AI 采用**分层微服务架构**，主要分为四个核心层：
┌─────────────────────────────────────────────────────────────────┐
│ 用户交互层 (UI/UX) │
│ • Web应用 (React) │
│ • 移动应用 (React Native) │
│ • 浏览器插件 │
├─────────────────────────────────────────────────────────────────┤
│ 应用服务层 (API) │
│ • 衣物管理服务 │
│ • 搭配生成服务 │
│ • 购物推荐服务 │
│ • 用户画像服务 │
├─────────────────────────────────────────────────────────────────┤
│ AI/ML 模型层 │
│ • 视觉识别Agent (CV Models) │
│ • 搭配推理Agent (LLM + Rules) │
│ • 电商推荐Agent (API Scraping + ML) │
├─────────────────────────────────────────────────────────────────┤
│ 基础设施层 │
│ • 数据存储 (MySQL + Redis) │
│ • 消息队列 (RabbitMQ) │
│ • 缓存服务 (Redis) │
│ • 文件存储 (S3/MinIO) │
└─────────────────────────────────────────────────────────────────┘

## 2. 核心服务设计

### 2.1 视觉识别Agent

**职责**：通过计算机视觉技术识别衣物属性

**技术栈**：
- **目标检测**：YOLOv8 + Faster R-CNN
- **图像分类**：ResNet50 + EfficientNet
- **颜色分析**：K-means聚类 + ColorThief
- **OCR**：Tesseract (用于识别标签)

**数据处理流程**：
```mermaid
graph TD
    A[用户上传照片] --> B[图像预处理]
    B --> C[目标检测]
    C --> D[衣物裁剪]
    D --> E[属性分类]
    E --> F[颜色提取]
    F --> G[材质识别]
    G --> H[结果聚合]
    H --> I[数据库存储]
